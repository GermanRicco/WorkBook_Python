#Pido los datos
N = int(input('Ingrese un numero entero de cuatro digitos: '))

#Hago los calculos
def obtenerSuma(N): #Funcion donde entro con el digito N y sale la suma de sus digitos
    suma = 0
    for digit in str(N):
        suma += int(digit)
    return suma

print(obtenerSuma(N))